{% for post in postList %}
    <article class="{{ "mb-8 lg:mb-16" if (not loop.last) }}">
        <h3 class="mt-0 mb-0 lg:mb-2 font-bold">
            <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h3>

        {% if post.data.excerpt %}
        <p class="my-2">{{ post.data.excerpt | md | safe }}</p>
        {% endif %}

        {% set categories = post.data.tags | categoryTags %}
        {% set date = post.data.date %}
        {% include 'blogPostDetails.njk' %}
    </article>
{% endfor %}
